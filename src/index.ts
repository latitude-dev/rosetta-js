/**
 * Rosetta - The translation layer for LLM provider messages
 *
 * This package provides utilities to convert messages between different LLM providers
 * (OpenAI, Anthropic, Google, Vercel AI SDK) using a standardized GenAI schema as
 * the intermediate format.
 */

export * from "$package/api";
